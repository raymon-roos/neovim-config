require("indent_blankline").setup({
  show_end_of_line = true,
  use_treesitter = false, -- Change once tree sitter has been set up
  show_current_context = false,
  show_current_context_start = false,
})
