require("indent_blankline").setup({
  show_end_of_line = true,
  use_treesitter = true, -- Change once tree sitter has been set up
  show_current_context = true,
  show_current_context_start = true,
})
